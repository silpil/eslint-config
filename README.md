### Install

```shell script
  npm install --save-dev @silpil/eslint-config
```

### Usage

- in package.json:

```json
{
  "eslintConfig": {
    "extends": [
      "@silpil/eslint-config"
    ]
  }
}
  
```  

### Peer dependencies

- eslint 6.x.x
- eslint-plugin-vue 6.x.x
